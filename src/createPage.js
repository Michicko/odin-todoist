const createPage = (function () {
  const page = () => {
    const pageHeader = document.createElement('header');
    
  }

  const getPage = (url, page) => {

  };

  return {
    getPage,
  };
})();


export default createPage;
